# 文档转换完整性规则

仅在 `knowledge-capture` 实际处理对应格式时读取相关章节。

## 通用整理

- 按源文件阅读顺序组织，不按主题重新拆卡。
- 保留标题、正文、列表、表格、公式文本、代码、脚注/尾注、题注、页码或幻灯片编号、讲者备注和图片位置。
- 允许合并同一段落的软换行、清除空白页与纯布局空行、规范 Markdown 转义。
- 重复页眉页脚只有在确认内容完全一致时可折叠，并在转换说明中记录；否则保留。
- 原文件始终是版式和不可转换对象的最终依据。Markdown 必须列出已知偏差，不能声称无损。

## Excel / CSV / TSV

- 默认转换全部工作表和已用区域，不抽样、不聚合、不去重。
- 保留工作表顺序、行列顺序、空值、公式单元格可取得的缓存实际值、错误值和合并区域；Markdown 不输出公式表达式。
- 公式单元格只读取源文件已保存的缓存值，不调用 Excel、LibreOffice 或其他工具重算。没有缓存值时在 Markdown 中留空，并在转换说明中列出对应工作表和单元格。
- 在每个工作表开头记录可见性、冻结窗格、筛选范围、隐藏行列、批注和合并区域；这些信息不必伪装成表格内容。
- 超宽或超长表格可以按工作表拆分为多个 Markdown 文件，但必须有一个 Inbox 索引文档，且能按原行列顺序恢复。
- 宏、图表、数据验证、条件格式和外部连接不能可靠转成 Markdown 时，列入转换说明；`.xlsm` 不得执行宏。

## Word

- 按文档块顺序保留段落、标题、列表、表格和内嵌图片。
- 处理页眉、页脚、脚注、尾注和批注；无法确定锚点时集中放在对应章节末尾并标出来源部件。
- 原图模式把图片紧跟其锚点段落或表格单元格；多模态模式把模型识别内容放在同一位置，标记图片位置与置信度。
- 文本框、SmartArt、公式对象、修订记录或域代码无法完整还原时明确列出，不得静默丢弃。

## PowerPoint

- 先区分源文件读取失败、文档包无效和单个对象提取失败。`PackageNotFoundError` 不能单独证明路径不存在或 PPT 损坏；文件被占用、无读取权限也可能导致第三方库给出该错误。通过统一启动器预检，不绕过预检直接调用 `Presentation(...)`；被占用时关闭对应应用后重试，不修改源文件。
- 保留幻灯片顺序；每页使用二级标题，并按从上到下、从左到右的视觉顺序整理对象。
- 保留标题、正文、列表、表格、图片、图表说明和讲者备注。
- 原图模式在对象对应位置嵌入；多模态模式在对象位置插入忠实的文字、表格、图表或示意关系识别结果。
- 提取普通图片、组合内图片和已填充的图片占位符。个别图片仅有外链、缺少内嵌引用、引用失效或数据无法读取时，继续处理其余对象和幻灯片；在缺图原位置及“转换说明”中记录幻灯片编号、对象编号和原因，不读取外链目标、不伪造图片或附件引用。需要恢复该图片时，由用户在原 PPT 中核对并嵌入后重新转换，转换器不修改源文件。
- 多模态模式只把成功提取的图片加入待识别清单；没有可提取图片时，直接保存文本和缺图说明。附件或笔记写入失败必须报错，不能当成可忽略的缺图。
- 动画、切换、视频、音频、嵌入对象和母版视觉效果无法转成 Markdown 时列入转换说明。

## PDF

- 保留页码边界，优先按页面文本块坐标恢复阅读顺序。
- 预检同时检查位图和可见矢量绘制路径，`vector_pages` 列出含矢量路径的页码；即使该页有原生文字，也应进入图文模式选择。
- 每页合并可见矢量路径所在区域，保留笔画边缘并扩展到完整的相交文字块、位图块，再渲染为 PNG。区域外的正文和图片照常提取；区域内的文字及位图随渲染区域保留，不再单独重复输出。原图模式嵌入该区域，多模态模式识别该区域并在对应位置插回。
- 同页多个矢量图可能合并为一个区域；背景或页框可能使区域扩展到整页。必须在转换说明中列出处理页码，不能声称保留了可编辑的矢量结构。
- 原图模式提取页面中的图片并插入对应文本块位置；扫描页无法分离图文时可把整页渲染为图片。
- 多模态模式对扫描页或图片块识别，按页面与坐标插回；保留页面、图片位置和置信度标签。
- 多栏、复杂表格、公式、矢量图和字符映射异常必须列入转换说明。多模态识别结果不是法律或审计意义上的原件。

## 多模态识别质量

- 只使用当前会话中明确支持图片输入的模型；模型不支持图片时停止并请用户切换模型，不调用 OCR 引擎或外部识别服务兜底。
- 保留段落和换行；表格保留行列；图表保留标题、轴、图例、数值和可直接观察的关系；示意图保留节点与连接。
- 不自行补全无法识别的字词、数值或关系。空结果、乱码或明显低置信时写 `[多模态模型未能可靠识别]`，并报告对应页/图片编号。
- 涉及金额、账号、日期、公式、专有名词或表格时，应重点核对；无法核对就标记不确定。
